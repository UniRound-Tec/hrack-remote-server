# 仅供 verify:p4-deployment 使用的 loopback HTTP 边缘。
# 生产必须使用 nginx.hrack.conf.example 的 TLS server。

map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}

server {
    listen 80;
    server_name _;
    absolute_redirect off;
    include /etc/nginx/hrack.routes.conf;
}
