# HRack 配对平台

HRack 配对平台让已认证用户持有并管理稳定的远程配对入口，同时把短暂的中继运行状态与用户拥有的持久身份分开。

## Language

**用户账号**：
平台中拥有认证身份和一条当前配对记录的主体。
_Avoid_：客户、租户

**配对记录**：
归属于一个用户账号、直到被轮换或吊销前一直存在的配对身份。
_Avoid_：房间、会话

**配对 URL**：
配对记录对外公开的稳定地址；服务重启不改变它，只有轮换才生成新地址。
_Avoid_：临时 URL、房间 URL

**运行时房间**：
Relay 中对某条有效配对记录的临时运行实例；它可以消失并从配对记录恢复，而不改变配对 URL。
_Avoid_：配对记录、配对 URL

**恢复**：
根据持久配对记录重新建立缺失运行时房间的过程。
_Avoid_：重新生成、轮换

**轮换**：
用户主动终止当前配对记录并创建新配对 URL 的操作。
_Avoid_：恢复、重启

**吊销**：
永久终止一条配对记录，使其配对 URL 不再恢复的操作。
_Avoid_：暂不可用、断线

**暂不可用**：
配对记录仍然有效，但对应运行时房间尚未恢复或 Relay 当前不可达的瞬时状态。
_Avoid_：已吊销、stale
